project Main is
   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Exec_Dir use "bin";

   for Main use ("main.adb");

   package Builder is
      for Default_Switches ("Ada") use ("-g");
      -- Commenting out the line causing issues
      -- for Switches ("Ada") use Zode8_Config.Build_Switches;
   end Builder;
end Main;